<div class="card shadow">
    <h5 class="card-header">
        Categories
    </h5>

    <div class="list-group list-group-flush">
        {{#each listCategories}}
        <a href="/products/{{ProID}}/categories"
            class="list-group-item list-group-item-action {{#if isActive}} active {{/if}}" id="category{{ProID}}">
            {{catName}}</a>
        {{/each}}

    </div>

</div>
